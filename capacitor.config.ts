import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.growtracker',
  appName: 'Cannabis Grow Tracker',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
